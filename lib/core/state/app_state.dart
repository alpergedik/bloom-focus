class AppState {
  static int water = 380;
  static double plantHeight = 2.4;
  static int completedSessions = 4;

  // Timer settings
  static int focusMinutes = 25;
  static int shortBreakMinutes = 5;
  static int longBreakMinutes = 15;

  // Preferences
  static bool notificationsEnabled = true;
  static bool soundEffectsEnabled = true;
  static bool darkModeEnabled = false;
}